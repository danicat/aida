fastapi
uvicorn
python-dotenv
huggingface_hub
sqlite-rag
litellm
google-genai
# Note: Ensure the correct package for google.adk is installed.
# It might be a preview package or require a specific install source.
google-adk
coverage
